#include <stdio.h>
#include <string.h>

int mains(int argc, char* argv[]){

    if(argc < 3){
        printf("Argumentos invalidos");
        return 1;
    }

    if(strcmp(argv[i], "maior")){
        if(argc < 4){
            printf("Argumentos invalido");
            return 1;
        }

        int x = ato1(argv[2]);
        int y = ato1(argv[3]);
        printf("%d", maior2n(x, y));
    }

    if(strcmp)

    return 0;
}